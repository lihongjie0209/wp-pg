# OpenHalo 架构说明

## 核心概念确认

**重要：OpenHalo 是一个完整的 PostgreSQL 内核 + MySQL 协议支持，不是代理层！**

## 什么是 OpenHalo？

OpenHalo 是 PostgreSQL 的一个修改版本（fork），它：
- ✅ **包含完整的 PostgreSQL 数据库引擎**
- ✅ **内置 MySQL 线协议支持**
- ✅ **包含 aux_mysql 扩展提供 MySQL 兼容函数**
- ❌ **不是**独立的代理或中间件
- ❌ **不需要**再安装标准的 PostgreSQL

## 技术架构

### 错误理解 ❌
```
WordPress → 代理层(OpenHalo) → PostgreSQL 数据库
```

### 正确架构 ✅
```
WordPress 
    ↓
    MySQL 协议连接 (端口 3306)
    ↓
OpenHalo (单一进程)
├── MySQL 协议监听器 (端口 3306)
├── PostgreSQL 协议监听器 (端口 5432)
├── SQL 兼容层
├── aux_mysql 扩展
└── PostgreSQL 数据库引擎
```

## 工作原理

### 1. 编译阶段
```bash
# 从 OpenHalo 源代码编译
git clone https://github.com/HaloTech-Co-Ltd/openHalo.git
cd openHalo
./configure --prefix=/home/halo/openhalo/1.0 --with-uuid=ossp --with-icu
make && make install
```

这会编译出：
- `postgres` 可执行文件（包含 MySQL 协议支持）
- `aux_mysql` 等扩展模块
- 所有标准的 PostgreSQL 工具（psql, pg_ctl 等）

### 2. 配置阶段

在 `postgresql.conf` 中配置：
```conf
# 启用 MySQL 兼容模式
database_compat_mode = 'mysql'

# 启用 MySQL 协议监听器
mysql.listener_on = true
mysql.port = 3306

# PostgreSQL 协议（仍然可用）
port = 5432
```

### 3. 运行阶段

启动单一的 OpenHalo 进程：
```bash
postgres -D $PGDATA
```

这个进程会：
- ✅ 在端口 5432 监听 PostgreSQL 协议
- ✅ 在端口 3306 监听 MySQL 协议
- ✅ 使用同一个数据存储引擎
- ✅ 提供 MySQL 和 PostgreSQL 两种访问方式

## 本项目的实现

### Docker Compose 结构

```yaml
services:
  openhalo:
    # 构建包含完整 PostgreSQL + MySQL 协议的 OpenHalo
    build: ./openhalo
    ports:
      - "5432:5432"  # PostgreSQL 协议（用于管理）
      - "3306:3306"  # MySQL 协议（WordPress 使用）
    
  wordpress:
    image: wordpress:latest
    environment:
      WORDPRESS_DB_HOST: openhalo:3306  # 连接到 MySQL 端口
```

### 关键点

1. **只有一个数据库容器**：`openhalo`
   - 不需要单独的 PostgreSQL 容器
   - 不需要代理容器

2. **两个协议端口**：
   - 5432: PostgreSQL 原生协议（可选，用于管理）
   - 3306: MySQL 协议（WordPress 使用）

3. **单一数据存储**：
   - 所有数据存储在 OpenHalo 的 PostgreSQL 引擎中
   - 通过两种协议访问的是同一份数据

## 与其他方案的对比

### PgBouncer (连接池) ❌
```
应用 → PgBouncer (代理) → PostgreSQL
```
- PgBouncer 是独立的代理进程
- 不提供协议转换

### MySQL Proxy ❌
```
应用 → MySQL Proxy → MySQL
```
- 是独立的中间件
- 用于 MySQL 的代理和路由

### OpenHalo ✅
```
应用 → OpenHalo (完整数据库)
```
- OpenHalo 本身就是数据库
- 内置多协议支持
- 单一进程，无额外跳转

## 验证方法

### 1. 查看进程
```bash
docker-compose exec openhalo ps aux | grep postgres
```

输出应该只显示一个 `postgres` 主进程及其子进程，没有独立的代理进程。

### 2. 查看版本
```bash
# PostgreSQL 协议
docker-compose exec openhalo psql -p 5432 -U halo -d postgres -c "SELECT version();"

# MySQL 协议（安装 mysql-client 后）
docker-compose exec openhalo mysql -h localhost -P 3306 -u halo -p -e "SELECT VERSION();"
```

两个命令返回的是同一个 PostgreSQL 版本信息。

### 3. 数据一致性验证
```bash
# 通过 PostgreSQL 协议插入数据
docker-compose exec openhalo psql -p 5432 -U halo -d postgres -c "CREATE TABLE test (id int, name text); INSERT INTO test VALUES (1, 'test');"

# 通过 MySQL 协议查询（安装 mysql-client 后）
docker-compose exec openhalo mysql -h localhost -P 3306 -u halo -p postgres -e "SELECT * FROM test;"
```

数据应该一致，因为它们访问的是同一个数据库引擎。

## 技术优势

### 集成式设计的优势

1. **性能更好**
   - 没有额外的网络跳转
   - 没有代理层的序列化/反序列化开销
   - 协议转换在内核层面完成

2. **运维更简单**
   - 只需要管理一个数据库进程
   - 统一的日志和监控
   - 减少故障点

3. **功能更强大**
   - 深度集成，不仅是协议转换
   - 可以优化 MySQL 特定的查询模式
   - 完整的 PostgreSQL 功能可用

## 配置文件说明

### postgresql.conf 关键配置

```conf
# 基础配置
port = 5432                              # PostgreSQL 协议端口
listen_addresses = '*'                   # 监听地址

# MySQL 兼容模式配置（OpenHalo 特有）
database_compat_mode = 'mysql'          # 启用 MySQL 兼容
mysql.listener_on = true                # 启用 MySQL 监听器
mysql.port = 3306                       # MySQL 协议端口
```

### 扩展加载

```sql
-- 创建 aux_mysql 扩展（提供 MySQL 函数）
CREATE EXTENSION aux_mysql CASCADE;
```

## 常见问题

### Q1: 为什么有两个端口？
**A:** OpenHalo 同时支持两种协议：
- 5432: PostgreSQL 协议（用于 DBA 管理、备份等）
- 3306: MySQL 协议（用于 MySQL 应用连接）

### Q2: 数据存储在哪里？
**A:** 数据存储在 OpenHalo 的 PostgreSQL 引擎中，存储格式是标准的 PostgreSQL 数据格式。

### Q3: 需要安装 MySQL 吗？
**A:** 不需要！OpenHalo 只是提供 MySQL 协议支持，不需要 MySQL 软件。

### Q4: 可以同时使用两种协议访问吗？
**A:** 可以！你可以：
- 用 MySQL 协议让应用连接（如 WordPress）
- 用 PostgreSQL 协议进行管理操作
- 访问的是同一份数据

### Q5: OpenHalo 是 PostgreSQL 的什么？
**A:** OpenHalo 是 PostgreSQL 的一个修改版本（fork），类似于：
- MariaDB 之于 MySQL
- Percona Server 之于 MySQL
- 但 OpenHalo 增加了 MySQL 协议支持

## 总结

**OpenHalo = PostgreSQL 内核 + MySQL 协议支持**

- ✅ 它是一个完整的数据库系统
- ✅ 不需要额外的 PostgreSQL 安装
- ✅ 不是代理或中间件
- ✅ 单一进程，多协议支持
- ✅ 基于官方 PostgreSQL 源代码修改而来

本项目中，我们只需要一个 OpenHalo 容器就能提供：
- PostgreSQL 数据库功能
- MySQL 协议兼容性
- WordPress 所需的全部数据库支持

## 参考资源

- [OpenHalo 官方仓库](https://github.com/HaloTech-Co-Ltd/openHalo)
- [OpenHalo 官方网站](https://www.openhalo.org/)
- [PostgreSQL 官方文档](https://www.postgresql.org/docs/)
