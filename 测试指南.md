# WordPress + OpenHalo 测试指南

## 项目简介

本项目演示如何使用 OpenHalo（PostgreSQL 的 MySQL 协议兼容层）运行 WordPress。这使得传统上需要 MySQL 的 WordPress 可以在 PostgreSQL 上运行，无需修改任何代码。

## 什么是 OpenHalo？

OpenHalo (https://www.openhalo.org/) 是由 HaloTech-Co-Ltd 开发的开源项目，它为 PostgreSQL 提供 MySQL 线协议兼容性。

**核心特点：**
- OpenHalo 是修改版的 PostgreSQL，内置 MySQL 协议支持
- 应用程序通过 MySQL 协议连接，无需代码修改
- 充分利用 PostgreSQL 的性能和功能优势
- 支持 MySQL 5.7+ 的常用语法和语义

## 系统架构

```
WordPress (端口 8080)
    ↓ MySQL 线协议 (端口 3306)
OpenHalo = PostgreSQL + MySQL 协议支持
    ↓ 内部转换
PostgreSQL 引擎 (端口 5432)
```

## 环境要求

- Docker 20.10 或更高版本
- Docker Compose 2.0 或更高版本
- 至少 4GB 可用内存
- 10GB 可用磁盘空间

## 快速开始

### 1. 克隆项目

```bash
git clone https://github.com/lihongjie0209/wp-pg.git
cd wp-pg
```

### 2. 启动服务

使用便捷脚本：
```bash
./start-demo.sh
```

或使用 Docker Compose：
```bash
docker-compose up -d --build
```

**注意：** 首次运行时，OpenHalo 需要从源代码编译，可能需要 10-20 分钟。

### 3. 检查服务状态

```bash
docker-compose ps
```

预期输出：
```
NAME                IMAGE               STATUS
wp-openhalo         wp-pg-openhalo      Up (healthy)
wp-app              wordpress:latest    Up (healthy)
```

### 4. 访问 WordPress

在浏览器中打开：http://localhost:8080

## 详细测试步骤

### 测试 1: 验证 OpenHalo 启动

```bash
# 检查 OpenHalo 日志
docker-compose logs openhalo

# 验证 PostgreSQL 协议
docker-compose exec openhalo psql -p 5432 -U halo -d postgres -c "SELECT version();"

# 验证 MySQL 兼容模式
docker-compose exec openhalo psql -p 5432 -U halo -d postgres -c "SHOW database_compat_mode;"
```

**预期结果：**
- 应显示 PostgreSQL 版本信息
- `database_compat_mode` 应显示为 `mysql`

### 测试 2: 验证 MySQL 协议

```bash
# 检查 MySQL 端口是否监听
docker-compose exec openhalo netstat -tlnp | grep 3306

# 或使用 nc 测试
docker-compose exec openhalo nc -zv localhost 3306
```

**预期结果：**
- 端口 3306 应处于 LISTEN 状态

### 测试 3: 安装 WordPress

1. 访问 http://localhost:8080
2. 选择语言（中文或英文）
3. 点击"继续"
4. WordPress 会自动检测数据库连接（通过 OpenHalo 的 MySQL 协议）
5. 填写站点信息：
   - 站点标题：WordPress + OpenHalo 演示
   - 用户名：admin
   - 密码：（设置强密码）
   - 电子邮件：your-email@example.com
6. 点击"安装 WordPress"

**预期结果：**
- WordPress 安装成功
- 能够登录管理后台
- 没有数据库连接错误

### 测试 4: 创建内容

1. 创建一篇文章：
   - 进入"文章" → "写文章"
   - 标题："你好，来自 PostgreSQL！"
   - 内容："这个 WordPress 站点运行在 PostgreSQL 上，通过 OpenHalo 的 MySQL 兼容层。不需要修改 WordPress 代码！"
   - 点击"发布"

2. 创建一个页面：
   - 进入"页面" → "新建页面"
   - 标题："关于本演示"
   - 添加内容描述项目
   - 点击"发布"

**预期结果：**
- 内容创建成功
- 可以在前台查看发布的内容

### 测试 5: 验证数据存储在 PostgreSQL

```bash
# 列出 WordPress 创建的所有表
docker-compose exec openhalo psql -p 5432 -U halo -d postgres -c "\dt"

# 查看文章数据
docker-compose exec openhalo psql -p 5432 -U halo -d postgres -c "SELECT post_title, post_status FROM wp_posts WHERE post_type = 'post';"

# 统计数据
docker-compose exec openhalo psql -p 5432 -U halo -d postgres -c "SELECT COUNT(*) FROM wp_posts;"
```

**预期结果：**
- 显示标准的 WordPress 数据表（wp_posts, wp_users, 等）
- 可以看到创建的文章标题
- 数据完整存储在 PostgreSQL 中

### 测试 6: 测试 WordPress 功能

#### 6.1 媒体上传
- 进入"媒体" → "添加"
- 上传一张图片
- 验证图片可以正常访问

#### 6.2 插件安装
- 进入"插件" → "安装插件"
- 搜索并安装简单插件（如 "Hello Dolly"）
- 激活并验证功能

#### 6.3 主题更换
- 进入"外观" → "主题"
- 激活不同的主题
- 访问前台验证主题已更换

### 测试 7: 性能观察

```bash
# 简单性能测试
time docker-compose exec openhalo psql -p 5432 -U halo -d postgres -c "SELECT COUNT(*) FROM wp_posts;"

# 生成测试数据（如果已安装 WP-CLI）
docker-compose exec wordpress wp post generate --count=50 --post_type=post --allow-root

# 再次查询验证
docker-compose exec openhalo psql -p 5432 -U halo -d postgres -c "SELECT COUNT(*) FROM wp_posts WHERE post_type = 'post';"
```

## 测试检查清单

完成以下所有项目表示测试成功：

- [ ] Docker 容器成功启动
- [ ] OpenHalo 正常运行，PostgreSQL 端口 (5432) 可访问
- [ ] MySQL 协议端口 (3306) 正在监听
- [ ] WordPress 安装成功完成
- [ ] WordPress 管理后台可访问
- [ ] 可以创建文章和页面
- [ ] 数据在 PostgreSQL 表中得到确认
- [ ] 媒体上传正常工作
- [ ] 插件可以安装和激活
- [ ] 主题可以更换
- [ ] 没有 WordPress 错误日志

## 常见问题排查

### 问题 1: OpenHalo 容器无法启动

**解决方法：**
```bash
# 查看构建日志
docker-compose build --no-cache openhalo

# 查看运行日志
docker-compose logs openhalo
```

### 问题 2: WordPress 显示"建立数据库连接时出错"

**解决方法：**
```bash
# 验证 OpenHalo 健康状态
docker-compose ps openhalo

# 检查 MySQL 端口
docker-compose exec openhalo netstat -tlnp | grep 3306

# 查看 OpenHalo 日志
docker-compose logs openhalo | tail -50
```

### 问题 3: 构建时间过长

**说明：** OpenHalo 需要从源代码编译 PostgreSQL，这是正常的。首次构建可能需要 10-20 分钟。

**查看进度：**
```bash
docker-compose build openhalo
```

## 清理环境

### 停止服务
```bash
docker-compose stop
```

### 删除容器和数据
```bash
docker-compose down -v
```

### 重新开始
```bash
docker-compose up -d --build
```

## 性能对比观察

### 优势场景
- ✅ 复杂 SQL 查询（JOIN、子查询）
- ✅ 高并发写入
- ✅ 大数据量统计
- ✅ 全文搜索

### MySQL 可能更优的场景
- 简单的单表查询
- 极低延迟要求的场景

## 技术细节

### OpenHalo 配置

OpenHalo 使用以下关键配置启用 MySQL 兼容性：

```conf
database_compat_mode = 'mysql'     # MySQL 兼容模式
mysql.listener_on = true           # 启用 MySQL 监听器
mysql.port = 3306                  # MySQL 协议端口
```

### 扩展模块

OpenHalo 包含 `aux_mysql` 扩展，提供：
- MySQL 函数兼容性
- MySQL 数据类型转换
- MySQL 语法支持

## 实际应用建议

### 适合使用 OpenHalo 的场景：

1. **新项目**：从头开始，希望使用 PostgreSQL 的高级功能
2. **企业环境**：已标准化使用 PostgreSQL
3. **高并发站点**：需要更好的并发处理
4. **数据分析需求**：需要复杂查询和分析功能

### 需要额外考虑的场景：

1. **简单博客**：MySQL 的简单性可能更合适
2. **共享主机**：通常无法自定义数据库
3. **插件依赖性强**：某些插件可能使用 MySQL 特定功能

## 参考资源

- [OpenHalo 官方网站](https://www.openhalo.org/)
- [OpenHalo GitHub 仓库](https://github.com/HaloTech-Co-Ltd/openHalo)
- [WordPress 官方文档](https://wordpress.org/documentation/)
- [PostgreSQL 官方文档](https://www.postgresql.org/docs/)

## 总结

通过本测试指南，您应该能够：

1. ✅ 成功部署 WordPress + OpenHalo 环境
2. ✅ 理解 OpenHalo 如何提供 MySQL 协议兼容性
3. ✅ 验证 WordPress 在 PostgreSQL 上的正常运行
4. ✅ 了解技术架构和工作原理
5. ✅ 评估是否适合您的使用场景

OpenHalo 为 MySQL 应用迁移到 PostgreSQL 提供了一个创新的解决方案，降低了迁移风险和成本。
